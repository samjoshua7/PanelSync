/**
 * Central config for <InfoTooltip /> messages.
 * Import TOOLTIP_MESSAGES and pass the relevant key's value as the message prop.
 */
export const TOOLTIP_MESSAGES = {
  fileUpload:
    "Supports JPG, PNG, WebP (max 5 MB) · MP4, WebM (max 50 MB). One file at a time.",
  pairCode:
    "Enter the 6-digit code shown on your TV screen. Codes expire in 2 minutes.",
  createEnv:
    "Environments group screens that share the same slideshow content. Max 4 per account.",
  textSlide:
    "Text slides display centered text with a custom font size, color, and background.",
};
